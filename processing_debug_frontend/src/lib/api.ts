export const API=process.env.NEXT_PUBLIC_PROCESSING_API_URL??"http://localhost:8000";
export type ModelStatus={component:string;checkpoint:string;device:string;expected_dimension:number|null;cache_available:boolean;loading_attempted:boolean;load_success:boolean;provider_kind:string;message:string};
export type Job={job_id:string;status:string;stage:string;progress:number;current_window:number;total_windows:number;elapsed_seconds:number;configuration:Record<string,unknown>;metadata:Record<string,unknown>;summary:Record<string,unknown>&{stage_durations?:Record<string,number>;stage_timing_semantics?:string};errors:{message:string;window_id?:string}[];model_status:ModelStatus[]};
export type WindowRow={index:number;window_id:string;start:number;end:number;transcript:string;change_scores:Record<string,number>;selected:boolean;selection_reasons:string[];vlm_call_state:string;caption:string;provenance:string;confidence:number;indexed:boolean;point_id:string|null;vectors:Record<string,{shape:number[];norm:number;min:number;max:number;finite:boolean}>;openai?:{request:unknown;response:unknown;usage:unknown}|null;errors:string[]};
export async function jsonFetch<T>(path:string,init?:RequestInit):Promise<T>{const response=await fetch(API+path,{...init,cache:"no-store"});if(!response.ok)throw new Error(await response.text());return response.json()}
