"use client";import {useEffect,useState} from "react";import {useParams} from "next/navigation";import {CartesianGrid,Legend,Line,LineChart,ReferenceLine,ResponsiveContainer,Tooltip,XAxis,YAxis} from "recharts";import {WindowRow,jsonFetch} from "@/lib/api";import {JobNav} from "@/components/JobNav";
export default function Timeline(){const {jobId}=useParams<{jobId:string}>();const [rows,setRows]=useState<WindowRow[]>([]);useEffect(()=>{jsonFetch<{windows:WindowRow[]}>(`/api/processing/jobs/${jobId}/windows`).then(x=>setRows(x.windows))},[jobId]);const data=rows.map(r=>({index:r.index,...r.change_scores,selected:r.selected?1:0,provenance:r.provenance}));return <main><h1>Change timeline</h1><JobNav id={jobId}/><section className="panel" style={{height:480}}><ResponsiveContainer><LineChart data={data}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="index"/><YAxis/><Tooltip/><Legend/><Line dataKey="visual" stroke="#2855d9"/><Line dataKey="audio" stroke="#16845b"/><Line dataKey="speech" stroke="#d17a00"/><Line dataKey="combined" stroke="#7b3fc6"/><Line dataKey="selected" stroke="#c23b3b"/><ReferenceLine y={.12} stroke="#2855d9" strokeDasharray="4 4"/><ReferenceLine y={.15} stroke="#16845b" strokeDasharray="4 4"/><ReferenceLine y={.18} stroke="#d17a00" strokeDasharray="4 4"/></LineChart></ResponsiveContainer></section><p className="muted">Selected markers include hard triggers and recovery selections; provenance is available in the window table.</p></main>}
